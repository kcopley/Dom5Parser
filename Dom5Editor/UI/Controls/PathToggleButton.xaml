<UserControl x:Class="Dom5Editor.UI.Controls.PathToggleButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="24" d:DesignWidth="24"
             x:Name="Root">

    <ToggleButton Width="22" Height="20" Margin="0,0,2,2" Padding="0"
                  IsChecked="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                  IsEnabled="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource InverseBoolConverter}}"
                  ToolTip="{Binding PathName, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <ToggleButton.Style>
            <Style TargetType="ToggleButton">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="Border" CornerRadius="3" BorderThickness="1"
                                    Background="{Binding UnselectedBackground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    BorderBrush="{Binding UnselectedBorder, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                <TextBlock x:Name="PathText"
                                           Text="{Binding Path, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           FontSize="10" FontWeight="Bold"
                                           Foreground="{Binding UnselectedForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Border" Property="Background"
                                            Value="{Binding SelectedBackground, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    <Setter TargetName="Border" Property="BorderBrush"
                                            Value="{Binding SelectedBorder, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    <Setter TargetName="PathText" Property="Foreground"
                                            Value="{Binding SelectedForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentHighlightBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Border" Property="Opacity" Value="0.6"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ToggleButton.Style>
    </ToggleButton>
</UserControl>
