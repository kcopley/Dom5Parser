<UserControl x:Class="Dom5Editor.UI.Controls.PropertyGroupControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">

    <Expander IsExpanded="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=UserControl}}"
              Style="{DynamicResource ExpanderBase}"
              Margin="0,0,0,12">
        <Expander.Header>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Group Title -->
                <TextBlock Grid.Column="0"
                           Text="{Binding Header, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           VerticalAlignment="Center"/>

                <!-- Separator Line -->
                <Rectangle Grid.Column="1"
                           Height="1"
                           Fill="{DynamicResource BorderBrush}"
                           Margin="12,0,12,0"
                           VerticalAlignment="Center"/>

                <!-- Optional Badge (property count, etc.) -->
                <Border Grid.Column="2"
                        Background="{DynamicResource SurfaceBrush}"
                        CornerRadius="8"
                        Padding="8,3"
                        Visibility="{Binding BadgeText, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource NullToVisibilityConverter}}">
                    <TextBlock Text="{Binding BadgeText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </Border>
            </Grid>
        </Expander.Header>

        <Border Padding="4,8,4,0">
            <!-- Content will be placed here -->
            <ContentPresenter Content="{Binding GroupContent, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Border>
    </Expander>
</UserControl>
