<UserControl x:Class="Dom5Editor.UI.Controls.CommandListEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="300"
             x:Name="Root">

    <StackPanel>
        <!-- Active Commands List -->
        <ItemsControl ItemsSource="{Binding ActiveCommands, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource BackgroundDarkBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="8,6"
                            Margin="0,0,0,4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Modified indicator (gold bar) -->
                            <Rectangle Grid.Column="0"
                                       Width="3"
                                       Fill="{DynamicResource AccentHighlightBrush}"
                                       Margin="0,0,8,0"
                                       RadiusX="1.5"
                                       RadiusY="1.5"
                                       Visibility="{Binding IsModified, Converter={StaticResource BoolToVisibilityConverter}}"
                                       ToolTip="Modified from vanilla"/>

                            <!-- Session edit indicator (cyan dot) -->
                            <Ellipse Grid.Column="1"
                                     Width="6"
                                     Height="6"
                                     Fill="{DynamicResource SessionEditBrush}"
                                     Margin="0,0,8,0"
                                     VerticalAlignment="Center"
                                     Visibility="{Binding IsSessionEdit, Converter={StaticResource BoolToVisibilityConverter}}"
                                     ToolTip="Added in this session"/>

                            <!-- Command Name -->
                            <TextBlock Grid.Column="2"
                                       Text="{Binding DisplayName}"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="12"/>

                            <!-- Inherited Badge -->
                            <Border Grid.Column="3"
                                    Background="{DynamicResource AccentSecondaryBrush}"
                                    CornerRadius="4"
                                    Padding="6,2"
                                    Margin="8,0"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding IsInherited, Converter={StaticResource BoolToVisibilityConverter}}"
                                    ToolTip="Inherited from copystats - cannot be removed">
                                <TextBlock Text="inherited"
                                           Foreground="White"
                                           FontSize="10"/>
                            </Border>

                            <!-- Remove Button -->
                            <Button Grid.Column="4"
                                    Content="✕"
                                    Width="24"
                                    Height="24"
                                    FontSize="12"
                                    Padding="0"
                                    VerticalAlignment="Center"
                                    Command="{Binding DataContext.RemoveCommandAction, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    IsEnabled="{Binding CanRemove}"
                                    ToolTip="{Binding RemoveTooltip}">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}"/>
                                                <Setter Property="Opacity" Value="0.5"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Add Command Dropdown (auto-adds on select) -->
        <ComboBox ItemsSource="{Binding AvailableCommands, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  DisplayMemberPath="DisplayName"
                  SelectedItem="{Binding SelectedCommandToAdd, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                  Style="{DynamicResource ComboBoxBase}"
                  Margin="0,4,0,0"
                  PreviewMouseWheel="OnComboBoxPreviewMouseWheel">
            <ComboBox.Tag>
                <TextBlock Text="+ Add..." Foreground="{DynamicResource TextSecondaryBrush}"/>
            </ComboBox.Tag>
        </ComboBox>
    </StackPanel>
</UserControl>
