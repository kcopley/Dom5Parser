<UserControl x:Class="Dom5Editor.UI.Controls.ReferencePropertyEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="32" d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="120"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Property Label -->
        <TextBlock Grid.Column="0"
                   Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   FontSize="12"
                   VerticalAlignment="Center"
                   Margin="0,0,12,0"/>

        <!-- Reference display/selection -->
        <Border Grid.Column="1"
                Background="{DynamicResource BackgroundDarkBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- ID display -->
                <TextBlock Grid.Column="0"
                           Text="{Binding ReferenceId, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat='#{0}'}"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="11"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"
                           Visibility="{Binding ReferenceId, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource NullToVisibilityConverter}}"/>

                <!-- Name display -->
                <TextBlock Grid.Column="1"
                           Text="{Binding ReferenceName, RelativeSource={RelativeSource AncestorType=UserControl}, TargetNullValue='(none)'}"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           FontSize="12"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
            </Grid>
        </Border>

        <!-- Browse button -->
        <Button Grid.Column="2"
                Content="..."
                Width="28"
                Margin="4,0,0,0"
                VerticalAlignment="Center"
                ToolTip="Browse for entity"
                Click="OnBrowseClick"/>

        <!-- Clear button -->
        <Button Grid.Column="3"
                Content="X"
                Width="28"
                Margin="4,0,0,0"
                VerticalAlignment="Center"
                ToolTip="Clear reference"
                Click="OnClearClick"
                Visibility="{Binding ReferenceId, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource NullToVisibilityConverter}}"/>
    </Grid>
</UserControl>
