<UserControl x:Class="Dom5Editor.Menus.IntPropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dom5Editor.Menus"
             mc:Ignorable="d" 
             d:DesignHeight="32">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Margin="2" Height="24" TextAlignment="Right" Text="{Binding Path=Label}"/>
        <TextBox Grid.Column="1" Height="24" Margin="2" Text="{Binding Path=Value, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"  TextAlignment="Center" Background="{Binding Path=BackgroundColor, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"/>
    </Grid>
</UserControl>
